{
  "name": "gemini-live-proxy",
  "version": "0.1.0",
  "description": "WebSocket proxy relaying audio + tool calls between the Expo app and the Gemini Live API",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  }
}
